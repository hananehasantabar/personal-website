<!DOCTYPE html>
<html lang="fa">
<head>
  <meta charset="UTF-8" />
  <title>???? ????? ???? ?????</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- ???? ?????? Leaflet -->
  <link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css" />
  <style>
    body {
      margin: 0;
      font-family: Tahoma, sans-serif;
      direction: rtl;
    }
    #map {
      height: 100vh;
      width: 100%;
    }
    .legend {
      background: white;
      padding: 10px;
      line-height: 1.6;
      font-size: 14px;
    }
    .legend span {
      display: inline-block;
      width: 12px;
      height: 12px;
      margin-left: 5px;
      vertical-align: middle;
    }
  </style>
</head>
<body>

<div id="map"></div>

<!-- ???????? Leaflet -->
<script src="https://unpkg.com/leaflet/dist/leaflet.js"></script>
<script>
  // ???????? ????? ????
  const map = L.map('map').setView([35.6892, 51.3890], 11); // ?????? ?????

  // ???? ????
  L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
    attribution: '&copy; OpenStreetMap contributors'
  }).addTo(map);

  // ???????? ????? ????? ???? (??????? ????? ??? ?? ?? ???? JSON ??? ???)
  const services = [
    {
      type: "education",
      name: "???????? ?????",
      coords: [35.7014, 51.4097],
      color: "blue"
    },
    {
      type: "fire_station",
      name: "??????? ????????? ????? ?????",
      coords: [35.6997, 51.3378],
      color: "red"
    },
    {
      type: "transport",
      name: "??????? ???? ??????",
      coords: [35.7191, 51.3381],
      color: "green"
    },
    {
      type: "communication",
      name: "???? ??????? ??????",
      coords: [35.7219, 51.4120],
      color: "orange"
    }
  ];

  // ???? ????? ???? Marker
  services.forEach(service => {
    const marker = L.circleMarker(service.coords, {
      color: service.color,
      radius: 8,
      fillOpacity: 0.9
    }).addTo(map);

    marker.bindPopup(`<strong>${service.name}</strong><br>???: ${service.type}`);
  });

  // ?????? ?????? (Legend)
  const legend = L.control({ position: "bottomright" });

  legend.onAdd = function (map) {
    const div = L.DomUtil.create("div", "legend");
    div.innerHTML += "<h4>??????? ??????</h4>";
    div.innerHTML += '<span style="background: blue"></span> ????? ? ?????<br>';
    div.innerHTML += '<spa
